interface:
  display_name: "SSH Key Provision"
  short_description: "Configure Linux SSH keys and install Codex CLI."
  default_prompt: "Use $ssh-key-provision to configure SSH key login and install Codex CLI for a Linux server; ask me for host, port, username, and password if needed."
policy:
  allow_implicit_invocation: true
